PROJECT_NAME=projectname
COMPOSE_PROJECT_NAME=${PROJECT_NAME}

# HOST PORT
WEB_HOST_PORT=
# NGINX_HOST_PORT=
MYSQL_HOST_PORT=

#NGINX
# NGINX_ROOT_DIR=/etc/nginx

#REDIS
REDIS_PORT=
REDIS_EXPOSED_PORT=
REDIS_LOG_DIR=/data/redis.log

WEB_VERSION=0.0.1-SNAPSHOT # version in build.gradle file
